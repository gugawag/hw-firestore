import { PrimeiroNomePipe } from './primeiro-nome.pipe';

describe('PrimeiroNomePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeiroNomePipe();
    expect(pipe).toBeTruthy();
  });
});
